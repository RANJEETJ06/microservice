# Normalize all text files to use LF
* text=auto eol=lf

# Explicit line endings for shell scripts and batch files
*.sh text eol=lf
*.cmd text eol=crlf
*.bat text eol=crlf

# Java source files
*.java text

# Markdown and docs
*.md text
*.adoc text

# JSON, YAML, XML, and properties
*.json text
*.yml text
*.yaml text
*.xml text
*.properties text

# Ignore differences in lock files or binary formats
*.jar binary
*.war binary
*.zip binary
*.png binary
*.jpg binary
*.jpeg binary
*.pdf binary
